extends ./main.pug
include ./mixins/video-mixin.pug

block content
    .shared__video-grid
        each item in videosDB
            +videoMixin(item)
        else
            span.shared__empty-message No videos found.

//-     /homeController\          main.pug
//-     --------------------         ^
//-    |                    |        ^
//-    | res.render('home') | > > home.pug
//-    |                    |
//-     --------------------
