mixin videoMixin(info)
  div
    h3
      a(href=`/videos/${info.id}`)= info.title
    p= info.description
    small= info.createdAt
    hr

//- text ===> #{}
//- attribute (ex. href, class, id) ===> `${}`
